# @package experiment
name: stage2_convlora_isic2016
output_root: ${oc.env:MEDSEG_OUTPUT_ROOT,./outputs}
tags: [stage2, convlora, adabn, isic2016]
save_top_k: 1
monitor: val_dice
monitor_mode: max
max_epochs: 50
log_every_n_steps: 10
checkpoint_every_n_epochs: 1
manifest_filename: run_manifest.json
qualitative_examples: 0
