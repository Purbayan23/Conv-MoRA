# @package experiment
name: bn_only_isic2017
output_root: ${oc.env:MEDSEG_OUTPUT_ROOT,./outputs}
tags: [stage2, bn_only, adabn, isic2017]
save_top_k: 0
monitor: none
monitor_mode: none
max_epochs: 5
log_every_n_steps: 10
checkpoint_every_n_epochs: 1
manifest_filename: run_manifest.json
qualitative_examples: 0
